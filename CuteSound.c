#include "CuteSound.h"
//cs_context_t* sound_context;
//cs_loaded_sound_t loaded_sound;
//cs_play_sound_def_t play_snd_def;
//cs_playing_sound_t* playing_snd;
void cute_init()
{

	//sound_context = cs_make_context(NULL, 48000, 4096, 0, NULL);
}

void cute_play(const char* path)
{
	//loaded_sound = cs_load_wav(path);
	//play_snd_def = cs_make_def(&loaded_sound);
	//cs_stop_all_sounds(sound_context);
	//playing_snd = cs_play_sound(sound_context, play_snd_def);
}

void cute_stop()
{
}

void cute_load(const char* path)
{
}
